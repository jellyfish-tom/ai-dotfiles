# Copilot implementation rules

Keep changes narrow. Validate with the smallest relevant check.

When region, brand, or flow scope is required, follow `.github/instructions/operator-context.instructions.md`.
